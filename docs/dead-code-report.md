# Dead-code candidates — frontend (static scan)

> Generated by `tools/dead-code-scan.mjs`. **Candidates for human review, NOT an auto-delete list.** A name is listed only if it appears **exactly once** in the entire frontend (its definition). Behavior dispatched by string (e.g. `data-act` handler names) can hide a real use, so verify each before removing. Names shorter than 4 chars are skipped.

## app.js

**APP-07** — §4 STATE & SESSIONS — one normalized object + the session model (SPEC §0.1)

- `matchesSearch` — app.js:2745

**APP-22** — §17 INTERNAL TEAM DOCK (Jac, Phase 7) — a bottom-bar chat built on the Phase-6

- `wrRailRemove` — app.js:9043
- `laneKeyOf` — app.js:9262

**APP-23** — §13.3 CARD GRAPH VIEW — RETIRED (2026-07-03). The per-card tile

- `gvWinCutoff` — app.js:9558

**APP-37** — §18b BACKEND SYNC — Google Sheets via the Apps Script web app

- `GPS_SHUTDOWN_ROLES` — app.js:20024

## config.js

_No unreferenced top-level symbols found._

## data.js

_No unreferenced top-level symbols found._

## cascade.js

_No unreferenced top-level symbols found._

## service-countdown.js

_No unreferenced top-level symbols found._

## agreements.js

_No unreferenced top-level symbols found._

## icons.js

_No unreferenced top-level symbols found._

---

_Scanned 1581 module-scope definitions; 5 unreferenced candidate(s)._

## Future: real execution coverage (follow-up)

Playwright already drives the app in CI (`smoke`, `logic-test`). A later step can collect JS **coverage** from those runs to show which chapters actually executed — real "active vs. cold" data, with zero production overhead and nothing added to `app.js`. Tracked as its own small spec.
