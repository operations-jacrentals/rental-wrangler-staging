# Handoff -> local Claude Code session (2026-06-18)

Pick up here. Live is current; this is what shipped from **this session only**, what is
intentionally temporary, and what to watch out for.

> **Concurrent sessions note:** Jac confirmed multiple Claude Code sessions have been
> editing `main` around the same time. The PRs listed here are strictly from this session.
> Always run `git log --oneline origin/main` to get the full live state before starting.

---

## What this session shipped (all on `main`, squash-merged via branch -> PR -> CI)

Theme of this session: **Blued Steel visual polish, navigation quality-of-life, and KPI masking.**

### 1 . Blued Steel theme suite (PRs #60/#61/#69/#72/#74/#77)

The full card surface is now a cohesive blued-steel data-plate across all three columns.

- **Search bar / `.searchwrap`** -- metal plate treatment matching the cards: gradient overlay +
  `tex-metal-blued.jpg` tiled at 300px, blued border + inset glow. Previously flat.
- **List cards** -- `tex-metal-blued.jpg` at 340px tile, blued-teal gradient overlay.
  Card cap hazard stripe is now yellow (`--stripe: var(--yellow)`).
- **Standard-view sections** (`.section`) -- milled-in recess: `rgba(11,15,24,.36)` + inset
  top glow + drop shadow. Sections read as wells machined into the plate.
- **Rows** (`.row`) -- same milled-panel recess formula. Row borders `rgba(150,178,222,.16)`.
- **Anchored cards** -- same plate formula, consistent with non-anchored state.

**Milled-panel recess formula** (for any dark panel surface in bluedsteel):
```css
background: rgba(11,15,24,.36);
box-shadow: inset 0 1px 0 rgba(150,178,222,.12), 0 2px 8px -3px rgba(0,0,0,.55);
```

### 2 . Back button -> list view fallback (PR #82)

`cardBack()` now returns to list view when stack is empty + card is in `standard` mode with a
record open. `cardJog()` shows Back even with empty stack when in a record.

### 3 . +Unit / +Category quick-add from empty search (PR #85)

Empty-search state now shows `+New Unit` / `+New Category` -- matching Rentals/Customers.
`quickAddUnitFromSearch` / `quickAddCategoryFromSearch` -> `openStandard()` (Back works).
ID generators: `nextUnitId()` -> U001...; `nextCategoryId()` -> CAT001...

### 4 . Column polish -- toggle gap + center column texture (PR #99)

**Toggle gap equalized.** `.card > .tabrow { margin: 13px 8px 8px; }` -- formula:
`desired_gap(8) + stripe_height(6) - border(1) = 13`. Exactly 8px above and below on all columns.

**Center column texture flip.** `assets/tex-metal-blued-flip.jpg` (horizontal mirror via
PowerShell RotateNoneFlipX) applied to the middle column -- breaks the "same pattern repeating"
perception for frequent users.

### 5 . KPI blur mask (PRs #132/#133 -- INTENTIONALLY LIVE, TEMPORARY)

```css
/* -- KPI MASK -- remove this block to restore ring visibility (Jac) -- */
.kpi-ring, .big-ring, .menu-team-ring { filter: blur(12px); pointer-events: none; }
/* -- END KPI MASK -- */
```

Very end of `style.css`. **Do NOT remove until Jac explicitly says so.**
PR #132 (color change) was a misread -- superseded by #133 (blur).

---

## What is permanent vs temporary

| Change | Status |
|---|---|
| Blued Steel card plates + search bar (PRs #60-#77) | Permanent |
| Back button -> list fallback (PR #82) | Permanent |
| +Unit / +Category quick-add (PR #85) | Permanent |
| Toggle gap equalized (PR #99) | Permanent |
| Center column texture flip -- tex-metal-blued-flip.jpg (PR #99) | Permanent |
| KPI blur mask (PR #133) | TEMPORARY -- remove when Jac says so |

---

## How to work safely

- Branch -> PR -> squash-merge always. `main` is branch-protected (required `smoke` CI check).
- Gates: `node ci/smoke.mjs` . `node ci/logic-test.mjs` . `node ci/gen-rule-usage.mjs --check`
- Port 8000 may be reserved. Swap: `sed -i 's/8000/9147/g' ci/smoke.mjs ci/logic-test.mjs`
- `wait-merge` syntax: `node rw-automation/gh-api.mjs wait-merge <branch-name> <pr-number>`
- Backend `Code.gs` -- never committed. backend/ is gitignored. Deployed via clasp at @19.

## Don'ts
- Never commit model identifier, secrets, or DEFAULT_CONFIG passwords (repo is public via Pages).
- Never push directly to `main` -- always branch -> PR.
- Do NOT remove the KPI blur mask until Jac explicitly instructs it.